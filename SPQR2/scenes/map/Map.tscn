[gd_scene load_steps=7 format=2]

[ext_resource path="res://gfx/map/map_final.png" type="Texture" id=1]
[ext_resource path="res://scenes/map/Map.gd" type="Script" id=2]
[ext_resource path="res://gfx/map/map_regions.png" type="Texture" id=3]
[ext_resource path="res://scenes/map_overlay/Overlay.tscn" type="PackedScene" id=7]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform sampler2D regions;
uniform vec4 region_color: hint_color;

void fragment() {
	vec4 rcol = texture(regions, UV);
	if(rcol.a == 0.0) {
		COLOR = texture(TEXTURE, UV);
	} else {
		if(rcol == region_color) {
			COLOR = vec4(1.0, 0.0, 0.5, 1.0);
		} else {
			COLOR = texture(TEXTURE, UV);
		}
	}
	/* 
	No idea why this does not work
	vec4 rcol = texture(regions, UV);
	if(rcol == region_color) {
		COLOR = vec4(1.0, 0.0, 0.5, 1.0);
	} else {
		COLOR = texture(TEXTURE, UV);
	}
	*/
}
"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/region_color = null
shader_param/regions = ExtResource( 3 )

[node name="Map" type="Node2D"]
script = ExtResource( 2 )

[node name="Map" type="Sprite" parent="."]
material = SubResource( 2 )
texture = ExtResource( 1 )
centered = false

[node name="Camera2D" type="Camera2D" parent="."]
position = Vector2( 1780, 1600 )
current = true
smoothing_enabled = true
smoothing_speed = 8.0

[node name="CanvasLayer" type="CanvasLayer" parent="."]

[node name="Overlay" parent="CanvasLayer" instance=ExtResource( 7 )]
